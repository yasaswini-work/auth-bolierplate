# Authentication Module (ExpressJS)

This folder shows how the tech-agnostic authentication template maps into
a concrete ExpressJS implementation.

## Included

- Controllers (register, login)
- Routes (`/auth/register`, `/auth/login`)
- DTO Validation
- In-memory Model (replaceable)
- Service Layer
- Config (token secrets)
- Tests (placeholder)
